typedef struct {
  bool boat_detected,
  bool bridge_motor_activated,
  bool bridge_raised
} bridge_properties_t;

#define MAIN_BRIDGE_MOTOR_PIN 1
#define SECONDARY_BRIDGE_MOTOR_PIN 2
#define BRIDGE_BOAT_SENSOR_PIN 3

// setup code for the bridge
void setup() {
  // initialize the sensors and variables
  
}

// program loop for the bridge -- this runs infinitely
void loop() {
  // run the loop and such
  
}
