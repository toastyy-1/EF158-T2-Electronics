typedef struct {
  bool bridge_sensor_activated,
  bool motor_running
} sensor_properties_t;

#define BRIDGE_SENSOR_PIN 1
#define MOTOR_R_PIN 2
#define MOTOR_L_PIN 3

// setup code for the car
void setup() {
  // initialize the sensors and variables

}

// program loop for the car -- this runs infinitely
void loop() {
  // run the loop and such
  
}
